# URL base de EV_Central (donde escucha el HTTP)
evw.central.baseUrl=http://127.0.0.1:8080

# API key de OpenWeather
evw.openweather.apiKey=

# Intervalo entre consultas (ms). La práctica habla de ~4s.
evw.pollMs=4000
